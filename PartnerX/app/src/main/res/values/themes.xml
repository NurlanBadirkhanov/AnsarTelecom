<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.PartnerX" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.PartnerX" parent="Base.Theme.PartnerX" />

    <style name="Button_ads" parent="Theme.AppCompat">
        <item name="android:clickable">true</item>
        <item name="android:contextClickable">true</item>
        <item name="android:foreground">?android:attr/selectableItemBackground</item>
        <item name="android:textColor">#FFFFFF</item>
        <item name="android:background">@drawable/background1</item>
        <item name="backgroundTint">@color/black</item>
    </style>

</resources>